# OpenClaw 项目架构分析报告

> **分析日期**: 2026-02-01  
> **项目版本**: 2026.1.30  
> **分析者**: 产品架构师 / 产品经理

---

## 1. 项目概述

### 1.1 产品定位

**OpenClaw** 是一个**本地优先 (Local-first) 的个人 AI 助手平台**，核心特点是让用户**在自己的设备上运行 AI 助手**，并通过多种消息渠道（WhatsApp、Telegram、Slack、Discord 等）与助手交互。

**核心价值主张**:
- 🏠 **本地运行**: 助手运行在用户自己的设备/服务器上
- 🔐 **隐私优先**: 数据不经过第三方托管
- 📱 **多渠道**: 一个助手，多个入口（IM、语音、Web）
- 🧩 **可扩展**: 插件系统支持自定义渠道和工具

### 1.2 技术栈摘要

| 层级 | 技术选型 |
|------|----------|
| **语言** | TypeScript (ESM), Swift (macOS/iOS), Kotlin (Android) |
| **运行时** | Node.js ≥22 |
| **构建工具** | pnpm (workspace), tsdown, tsgo, Vitest |
| **包管理** | pnpm workspace monorepo |
| **协议** | WebSocket (Gateway 协议), HTTP REST |
| **AI 后端** | Anthropic Claude, OpenAI, Google Gemini, 本地 Ollama |
| **前端 UI** | Lit + Vite (Web), SwiftUI (macOS/iOS), Jetpack Compose (Android) |

---

## 2. 系统架构

### 2.1 高层架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            外部消息渠道                                       │
│  WhatsApp │ Telegram │ Slack │ Discord │ Signal │ iMessage │ Teams │ ...    │
└───────────────────────────────────┬─────────────────────────────────────────┘
                                    │
                                    ▼
┌───────────────────────────────────────────────────────────────────────────────┐
│                           Gateway (控制平面)                                    │
│                          ws://127.0.0.1:18789                                  │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌────────────────────────┐  │
│  │ 会话管理     │ │ 通道管理     │ │ 工具注册     │ │ 事件广播/Cron 调度      │  │
│  └─────────────┘ └─────────────┘ └─────────────┘ └────────────────────────┘  │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌────────────────────────┐  │
│  │ 认证/配对    │ │ 扩展/插件    │ │ 安全控制     │ │ HTTP/WS API           │  │
│  └─────────────┘ └─────────────┘ └─────────────┘ └────────────────────────┘  │
└───────────────────────────────────┬─────────────────────────────────────────┘
                                    │
          ┌─────────────────────────┼─────────────────────────┐
          ▼                         ▼                         ▼
    ┌──────────┐            ┌──────────────┐           ┌──────────────┐
    │ Pi Agent │            │  CLI 工具     │           │ 原生应用      │
    │  (RPC)   │            │ (openclaw)   │           │ macOS/iOS/   │
    └──────────┘            └──────────────┘           │ Android      │
                                                       └──────────────┘
```

### 2.2 核心组件

#### 2.2.1 Gateway (网关)

**位置**: `src/gateway/`

Gateway 是整个系统的**控制平面**，负责：

- **WebSocket 服务器**: 提供实时双向通信
- **会话管理**: 创建、维护、路由 AI 对话会话
- **通道连接**: 管理各消息平台的连接（Baileys for WhatsApp, grammY for Telegram 等）
- **工具调度**: 注册和调用 Agent 工具
- **事件广播**: Cron 任务、Webhook、心跳等自动化

**协议设计** (`src/gateway/protocol/`):
- 使用 JSON-RPC 风格的请求/响应帧
- TypeBox 定义的严格 Schema 验证
- 支持的主要方法：`chat.send`, `sessions.*`, `config.*`, `nodes.*` 等

#### 2.2.2 Agent (智能体)

**位置**: `src/agents/`

Agent 是执行 AI 推理的核心，基于 [pi-mono](https://github.com/badlogic/pi-mono) 的嵌入式运行时：

- **Pi Embedded Runner**: 本地嵌入式 Agent 运行时
- **模型适配**: 支持 Anthropic, OpenAI, Google AI, Bedrock, Ollama 等
- **工具执行**: Bash 执行、浏览器控制、Canvas 渲染
- **上下文管理**: 会话压缩 (Compaction)、历史窗口
- **Sandbox**: Docker 隔离执行环境

#### 2.2.3 Channels (消息通道)

**核心通道** (`src/telegram/`, `src/discord/`, `src/slack/` 等):

| 通道 | 实现库 | 状态 |
|------|--------|------|
| WhatsApp | @whiskeysockets/baileys | ✅ 内置 |
| Telegram | grammY | ✅ 内置 |
| Slack | @slack/bolt | ✅ 内置 |
| Discord | discord.js + @buape/carbon | ✅ 内置 |
| Signal | signal-cli | ✅ 内置 |
| iMessage | imsg (macOS 专属) | ✅ 内置 |
| Google Chat | Chat API | ✅ 内置 |

**扩展通道** (`extensions/`):

| 通道 | 描述 |
|------|------|
| Microsoft Teams | Bot Framework 集成 |
| Matrix | E2EE 加密支持 |
| Zalo / Zalo Personal | 越南本地化 |
| BlueBubbles | iMessage 桥接 |
| LINE | Messaging API |
| Twitch | 直播聊天 |
| Nostr | 去中心化协议 |
| Tlon/Urbit | Urbit 平台 |

#### 2.2.4 原生应用

**macOS 应用** (`apps/macos/`):
- 菜单栏控制面板
- Voice Wake (语音唤醒) + PTT (按键说话)
- Talk Mode 覆盖层
- 远程 Gateway 控制

**iOS 应用** (`apps/ios/`):
- Canvas 渲染
- Voice Wake + Talk Mode
- Bonjour 配对

**Android 应用** (`apps/android/`):
- Canvas + Camera
- SMS 集成 (可选)
- Talk Mode

---

## 3. 功能模块分析

### 3.1 核心功能

#### 3.1.1 多渠道收件箱

```
用户消息 (WhatsApp/TG/Discord/...)
         │
         ▼
┌─────────────────┐      ┌────────────────┐
│ Channel Adapter │ ───► │ Session Router │
└─────────────────┘      └───────┬────────┘
                                 │
                                 ▼
                         ┌───────────────┐
                         │ Agent Session │
                         └───────────────┘
```

**关键特性**:
- 一个 Agent 可同时连接多个渠道
- 会话隔离: 按联系人/群组/话题独立管理
- DM 配对机制: 未知发送者需通过配对码授权
- 群组激活: 支持 `@mention` / `always` 模式

#### 3.1.2 语音交互

**Voice Wake** (`src/agents/`, 原生应用):
- ElevenLabs TTS 集成
- 持续语音监听
- PTT (Push-to-Talk) 模式

**Talk Mode**:
- 持续对话模式
- 多平台支持 (macOS/iOS/Android)

#### 3.1.3 工具系统

**内置工具** (`src/agents/tools/`):

| 工具类别 | 功能 |
|----------|------|
| `exec` | Shell 命令执行，支持 PTY |
| `browser` | Playwright 浏览器控制 |
| `canvas` | A2UI 可视化工作区 |
| `web_fetch` / `web_search` | 网络请求和搜索 |
| `sessions_*` | 跨会话通信 |
| `cron` | 定时任务 |
| `message` | 向通道发送消息 |
| `nodes` | 设备节点操控 |

**Skills 系统**:
- 工作区技能: `~/.openclaw/workspace/skills/`
- 托管技能: npm 安装
- 捆绑技能: 内置 Git/MCP/等

#### 3.1.4 Canvas (A2UI)

**位置**: `src/canvas-host/`

Agent 驱动的可视化工作区:
- 动态 HTML/CSS/JS 渲染
- 快照、重置、Eval 操作
- 跨平台 Canvas 客户端

### 3.2 运维功能

#### 3.2.1 守护进程

```bash
# macOS: launchd
# Linux: systemd
# Windows: Task Scheduler

openclaw onboard --install-daemon
```

#### 3.2.2 Tailscale 集成

- `serve`: 仅 Tailnet HTTPS
- `funnel`: 公网 HTTPS (需密码认证)

#### 3.2.3 Docker 支持

```yaml
# docker-compose.yml 支持
# Sandbox: per-session Docker 隔离
agents.defaults.sandbox.mode: "non-main"
```

---

## 4. 目录结构详解

```
openclaw/
├── apps/                      # 原生应用
│   ├── android/               # Android 节点
│   ├── ios/                   # iOS 节点
│   ├── macos/                 # macOS 菜单栏应用
│   └── shared/                # 共享 Swift/Kotlin 代码 (OpenClawKit)
│
├── docs/                      # Mintlify 文档源
│
├── extensions/                # 渠道/功能扩展 (插件)
│   ├── bluebubbles/
│   ├── googlechat/
│   ├── matrix/
│   ├── msteams/
│   ├── voice-call/
│   └── ...                    # ~30 个扩展
│
├── packages/                  # 内部 NPM 包
│   ├── clawdbot/              # 兼容性 shim
│   └── moltbot/               # 兼容性 shim
│
├── scripts/                   # 构建/发布脚本
│
├── skills/                    # 捆绑技能
│
├── src/                       # 核心源码 (~2500 文件)
│   ├── agents/                # Pi Agent 运行时 (441 文件)
│   ├── gateway/               # Gateway WS 服务器 (189 文件)
│   ├── channels/              # 通道共享逻辑
│   ├── cli/                   # CLI 命令
│   ├── config/                # 配置管理
│   ├── browser/               # Playwright 工具
│   ├── cron/                  # 定时任务
│   ├── discord/               # Discord 连接器
│   ├── telegram/              # Telegram 连接器
│   ├── slack/                 # Slack 连接器
│   ├── signal/                # Signal 连接器
│   ├── imessage/              # iMessage 连接器
│   ├── web/                   # Control UI + WebChat
│   ├── memory/                # 向量记忆搜索
│   ├── plugins/               # 插件加载器
│   ├── security/              # 安全审计
│   └── ...
│
├── ui/                        # Control UI (Lit + Vite)
│
├── package.json               # monorepo 根配置
├── pnpm-workspace.yaml        # pnpm workspace
└── vitest.*.config.ts         # 测试配置
```

---

## 5. 数据流与交互模式

### 5.1 消息入站流程

```
1. 外部消息 (WhatsApp/TG/...)
        │
        ▼
2. Channel Connector (baileys/grammy/bolt)
        │
        ▼
3. Gateway Router
   - 解析会话键 (session key)
   - 检查 allowlist/pairing
   - 路由到 Agent
        │
        ▼
4. Pi Agent Run
   - 加载会话历史
   - 构建 System Prompt
   - 调用 LLM Provider
   - 执行工具
        │
        ▼
5. 响应流式输出
   - Block Streaming
   - 分块发送到通道
```

### 5.2 Gateway WebSocket 协议

**连接建立**:
```json
{
  "method": "hello",
  "params": {
    "protocol": "2024.3",
    "clientType": "cli",
    "token": "xxx"
  }
}
```

**RPC 请求**:
```json
{
  "id": 1,
  "method": "chat.send",
  "params": {
    "sessionId": "main",
    "content": "Hello!",
    "agentId": "default"
  }
}
```

**事件推送**:
```json
{
  "event": "chat",
  "data": {
    "type": "text.delta",
    "text": "Hi there!"
  }
}
```

---

## 6. 安全模型

### 6.1 认证层级

| 层级 | 机制 |
|------|------|
| Gateway 访问 | Token / Password / Tailscale Identity |
| DM 配对 | 配对码审批 (`dmPolicy: "pairing"`) |
| 工具执行 | 白名单 / 审批制 / Sandbox |
| 设备节点 | 配对 + 加密令牌 (Keychain) |

### 6.2 Sandbox 隔离

```yaml
agents:
  defaults:
    sandbox:
      mode: "non-main"     # 非主会话启用隔离
      allow: [bash, read]
      deny: [browser]
```

---

## 7. 可扩展性设计

### 7.1 插件架构

```
extensions/<plugin-name>/
├── package.json          # { "main": "dist/index.js" }
├── src/
│   └── index.ts          # 插件入口
└── manifest.json         # 渠道/工具元数据
```

**插件 SDK** (`src/plugin-sdk/`):
- 渠道注册
- 工具注册
- OAuth Provider 注册

### 7.2 Skills 系统

```
~/.openclaw/workspace/skills/<skill>/
├── SKILL.md              # 技能说明
├── scripts/              # 可执行脚本
└── resources/            # 资源文件
```

---

## 8. 近期版本更新亮点 (2026.1.x)

| 版本 | 重点更新 |
|------|----------|
| 2026.1.30 | Shell 自动补全, MiniMax OAuth, Kimi K2.5 |
| 2026.1.29 | 重命名为 `openclaw`, Gateway 认证强化 |
| 2026.1.24 | Ollama 发现, LINE 插件, Edge TTS 回退 |
| 2026.1.23 | TTS 核心化, `/tools/invoke` HTTP API, Tlon 插件 |
| 2026.1.22 | 压缩自适应分块, 嵌入批量处理 |
| 2026.1.20 | 混合 BM25+向量搜索, Nostr 插件, ACP |

---

## 9. 架构优势与改进建议

### 9.1 架构优势

- ✅ **本地优先**: 数据留在用户设备，隐私有保障
- ✅ **灵活部署**: 本地/Linux 服务器/Docker/Fly.io/Render 多选
- ✅ **多渠道统一**: 一套代码，多平台触达
- ✅ **可扩展**: 插件机制支持社区贡献
- ✅ **工程质量**: 完善的测试 (70% 覆盖率门槛)、类型安全

### 9.2 改进建议

| 领域 | 建议 |
|------|------|
| 文档 | 添加中文文档 |
| 插件生态 | 建立官方插件市场 (类似 Homebrew Cask) |
| 监控 | 添加可观测性仪表盘 (Grafana/Prometheus) |
| 多租户 | 支持多用户隔离 (适合家庭/小团队) |

---

## 10. 总结

OpenClaw 是一个**设计精良、功能完备的个人 AI 助手平台**：

1. **产品层面**: 真正的"本地 AI 助手"，解决了隐私和数据主权问题
2. **技术层面**: 现代化 TypeScript 全栈架构，monorepo 组织清晰
3. **生态层面**: 13+ 消息通道插件，覆盖主流 IM 平台
4. **工程层面**: 自动化测试、严格类型检查、持续集成

此项目适合追求**隐私保护**、**高度定制化**、**多平台触达**的技术用户。

---

*报告生成时间: 2026-02-01*
